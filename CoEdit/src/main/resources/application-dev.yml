spring:
  datasource:
    url: jdbc:mysql://localhost:3306/coedit?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: ${DB_PASSWORD:xxwssl}
    driver-class-name: com.mysql.cj.jdbc.Driver
redis:
     host: localhost
     port: 6379
     password: ${REDIS_PASSWORD:xxwssl}
     database: 0
     lettuce:
       pool:
         max-active: 8
         max-idle: 8
         min-idle: 2

mybatis-plus:
  configuration:
    log-impl: ${MYBATIS_LOG:org.apache.ibatis.logging.stdout.StdOutImpl}
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto