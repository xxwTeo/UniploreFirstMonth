server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/coedit?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: "xxwssl"
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      password: "xxwssl"
      port: 6379
      database: 0
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
